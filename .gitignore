.idea
.fleet
*.iml
.DS_Store

# Build output. Anchored, so a source package that happens to be called
# `target` or `build` is not swallowed along with it.
/target/
/plugin/target/
/plugin/build/
/legacy/*/build/
/legacy/*/.idea/

.claude/
.consulo
plugin/sandbox/
